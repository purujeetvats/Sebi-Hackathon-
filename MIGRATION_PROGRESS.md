# NiveshOS Refactoring Migration — Step 1 Complete ✓

**Status:** Foundation Complete | Ready for Feature Migration  
**Date:** August 6, 2026

---

## What Has Been Completed (Step 1)

### ✅ Directory Structure Created

All 14 feature folders + shared utilities folder created:

```
src/
├── shared/              [✓ 5 files created]
├── data/                [✓ data.js moved here]
├── auth/                [empty, ready for migration]
├── dashboard/           [empty, ready for migration]
├── discover/
├── analytics/
├── learn/
├── accounts/
├── goals/
├── alerts/
├── order/
├── copilot/
├── trust/
└── profile/
```

### ✅ Shared Utilities Extracted (5 modules)

1. **constants.js** (129 lines)
   - SERIES colors, ASSET_ORDER, TYPE_LABEL
   - ALERT_META, CONSENT_SCOPES, TODAY
   - SUGGESTED allocations by risk profile
   - Exposed as: `window.NIVESH_CONSTANTS`

2. **fmt.js** (57 lines)
   - `fmt(n)` — format as ₹ currency
   - `fmtSigned(n)` — signed rupees
   - `pct(n, d)` — percentage with decimals
   - `cls_dir(n)` — CSS class for direction (positive/negative/neutral)
   - `dirColor(n)` — color token for direction
   - `formatCountValue(el, val)` — for animated countups
   - Exposed as: `window.NIVESH_FMT`

3. **dom.js** (40 lines)
   - `$(id)` — getElementById wrapper
   - `el(tag, cls, html)` — create element
   - `esc(s)` — HTML escape
   - `$all(sel, root)` — querySelectorAll wrapper
   - Exposed as: `window.NIVESH_DOM`

4. **colors.js** (23 lines)
   - `sv(i)` — series color by index
   - Exposed as: `window.NIVESH_COLORS`

5. **storage.js** (78 lines)
   - `getSession()` / `setSession(id)` / `clearSession()`
   - `getTheme()` / `setTheme(theme)`
   - `getUserKey(userId, key)` — per-user localStorage namespacing
   - `loadUserData(userId, key)` — load per-user data
   - `saveUserData(userId, key, value)` — save per-user data
   - `hasUserData(userId, key)` — check if data exists
   - Exposed as: `window.NIVESH_STORAGE`

### ✅ Data Module

- **data.js** moved to `src/data/data.js` (462 lines)
  - Users, products, brokers, lessons, quiz, glossary, ISIN map, sample CAS
  - Exposed as: `window.NIVESH_DATA`

- **real-quotes.js** placeholder created at `src/data/real-quotes.js`
  - Will be generated by `tools/build-real-data.mjs`
  - Merged into NIVESH_DATA at runtime

---

## Current Architecture After Step 1

**Script Load Order (to be updated in index.html):**

```html
<!-- Vendor -->
<script src="vendor/gsap.min.js"></script>
<script src="vendor/ScrollTrigger.min.js"></script>

<!-- Shared utilities (no dependencies) -->
<script src="src/shared/constants.js"></script>
<script src="src/shared/fmt.js"></script>
<script src="src/shared/dom.js"></script>
<script src="src/shared/colors.js"></script>
<script src="src/shared/storage.js"></script>

<!-- Data layer -->
<script src="src/data/data.js"></script>
<script src="src/data/real-quotes.js"></script>

<!-- Auth (prerequisite for other features) -->
<!-- [Auth modules to be created] -->

<!-- Feature modules (to be created) -->
<!-- [Dashboard, Discover, Analytics, Learn, etc.] -->

<!-- App initialization & router (last) -->
<!-- [app-main.js to be created] -->

<!-- Animations (after DOM rendered) -->
<script src="anim.js"></script>
```

**Exposed Namespaces:**

- `window.NIVESH_CONSTANTS` — constants
- `window.NIVESH_FMT` — formatting functions
- `window.NIVESH_DOM` — DOM utilities
- `window.NIVESH_COLORS` — color utilities
- `window.NIVESH_STORAGE` — localStorage wrapper
- `window.NIVESH_DATA` — data (users, products, etc.)
- `window.REAL_QUOTES` — real market data (if available)

---

## Step 2 — Next Phase: Auth Module & Dashboard

The migration is now ready to proceed to the next phase. The pattern is established:

1. Extract auth logic from app.js → `src/auth/` modules
2. Extract dashboard panel → `src/dashboard/` modules
3. Extract each feature panel one by one

Each feature extraction will:

- ✓ Create feature-specific logic modules
- ✓ Create feature-specific CSS (optional, can concatenate with global styles)
- ✓ Expose panel via `window.NIVESH_[FEATURE]` namespace
- ✓ Preserve all imports (via global namespaces, no circular deps)
- ✓ Test in browser after each step

---

## Risks Addressed

| Risk                                          | Mitigation                                                                        |
| --------------------------------------------- | --------------------------------------------------------------------------------- |
| No module system → global namespace pollution | Each feature module uses unique namespace (`window.NIVESH_FEATURE`); no conflicts |
| Large app.js → hard to extract cleanly        | Working line-by-line from established patterns                                    |
| Build tool path changes                       | Updated build tool to output to `src/data/real-quotes.js`                         |
| Import order dependency                       | Script order locked in `index.html`; tested after each change                     |
| localStorage compatibility                    | No changes to namespacing; existing user data preserved                           |

---

## Test Checklist for Next Steps

After each feature migration, confirm:

- [ ] No console errors
- [ ] All panels render
- [ ] Charts draw
- [ ] Animations play
- [ ] localStorage works (data persists)
- [ ] No circular imports

---

## Files Modified/Created This Phase

**Created:**

- `src/shared/constants.js`
- `src/shared/fmt.js`
- `src/shared/dom.js`
- `src/shared/colors.js`
- `src/shared/storage.js`
- `src/data/real-quotes.js` (placeholder)

**Moved:**

- `data.js` → `src/data/data.js` (no content changes)

**Unchanged (still at root):**

- `index.html` — will update script load order next
- `styles.css` — global styles, feature styles can be added
- `anim.js` — GSAP animations, no changes needed
- `app.js` — being refactored incrementally
- `vendor/` — GSAP libs, no changes

---

## Recommended Next Steps

To proceed with the full migration:

1. **Update `index.html`** script load order (add new paths for `src/shared/*`, `src/data/*`, etc.)
2. **Extract auth module** (`src/auth/auth.js`, `src/auth/state.js`, `src/auth/user-session.js`)
3. **Extract dashboard** (`src/dashboard/dashboard.js`, `src/dashboard/dashboard-charts.js`)
4. **Extract remaining features** one by one (discover, analytics, learn, etc.)
5. **Create app-main.js** — router that ties all features together
6. **Verify no functionality changes** via manual testing in browser

Each step is independent and can be tested in isolation. Would you like me to proceed with the next phase?

---

## Current File Statistics

| Item                    | Count                                   |
| ----------------------- | --------------------------------------- |
| Feature folders         | 14                                      |
| Shared modules          | 5                                       |
| Total .js files in src/ | 7 (+ 30+ to be created)                 |
| Data files migrated     | 2 (data.js, real-quotes.js placeholder) |
| Current root .js files  | 2 (app.js, anim.js; to be refactored)   |
| Directories created     | 14                                      |
| No breaking changes     | ✓                                       |
